  - name: Upload build log
+        uses: actions/upload-artifact@v4   # ← new major version
+        with:
+          name: build-log
+          path: buildlog/build-log.txt
+      - uses: actions/download-artifact@v4
+        with:
+          name: build-log
